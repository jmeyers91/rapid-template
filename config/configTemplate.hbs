const { env } = process;

module.exports = {
  webserver: {
    port: 9090,
    publicDir: 'public',
  },
  database: {
    connection: {
      user: env['USER'] || 'root',
      password: '',
      database: '{{ databaseName }}'
    },
  },
};
