const { env } = process;

module.exports = {
  logActions: true,
  jwtDuration: 1000 * 60 * 60 * 24 * 30, // 1 month
  webserver: {
    port: 9090,
    publicDir: 'public',
  },
  database: {
    connection: {
      user: env['USER'] || 'root',
      password: '',
      database: '{{ databaseName }}'
    },
  },
};
