
exports.up = async knex => {
  if (await knex.schema.hasTable('{{snakeCase name}}')) return;
  return knex.schema.createTable('{{snakeCase name}}', table => {
    table.increments('id').primary();
    table.timestamps(true, true);

    // Relation example:
    // table
    //   .integer('author_id')
    //   .references('id')
    //   .inTable('users')
    //   .onDelete('CASCADE')
    //   .notNullable();
  });
};

exports.down = async knex => {
  if (await knex.schema.hasTable('{{snakeCase name}}')) {
    return knex.schema.dropTable('{{snakeCase name}}');
  }
};
