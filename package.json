{
  "name": "rapid-app",
  "version": "1.0.27",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npx rapid",
    "watch": "npx rapid watch",
    "clear": "npx rapid clear",
    "migrate": "npx rapid migrate",
    "seed": "npx rapid seed",
    "refresh": "npx rapid clear migrate seed",
    "update:rapid": "npm install @simplej/rapid@latest @simplej/rapid-cli@latest",
    "generate": "npx plop",
    "format": "npx prettier --single-quote --write \"{config,controllers,hooks,migrations,models,actions,routes,routers,seeds,tests}/**/*.js\"",
    "test": "export NODE_ENV='test' && npx jest"
  },
  "license": "ISC",
  "dependencies": {
    "@simplej/rapid": "0.0.61",
    "@simplej/rapid-cli": "0.0.43",
    "@simplej/rapid-plop": "^1.0.0",
    "npx": "^10.2.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "jest": "^23.4.2",
    "plop": "^2.0.0",
    "prettier": "^1.14.0"
  }
}
